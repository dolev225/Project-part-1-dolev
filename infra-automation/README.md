=========================================================
welcome to the infar_automation project v1.0
=========================================================
over view :
the code installing pydantic & Nginx  and get the machine
info from the user and store the data at the 
folder configs within file instances.json
all so create a log file at folder logs logs.log
=========================================================
setup & execution instruction :
1) open the folder "src" and run the python script "infra_simulator.py" 
2)enter the admin code to install the items and wait 
to the installer to finsh if done correctly you should receive a msg the install is done 
3)if you wish to stop type "stop" if will stop the code and wont save the data in to the file || perss enter to continue
4)now you need to enter the meashin info (name,os,ram,cpu)
    *****pleas note that 
    *****name mast be less the 20 
    *****os mast be 'windows' or 'ubuntu' 
    *****ram up too 1024kb (enter jsut the number without kb)
    *****cpu up too 64 
5)now the machine info is save at the instances.json you can see it there 
6) if the code failed you can go to the log file and see what happened there 
=========================================================
report:
if you see any bug or you wish to report en error
pleas send the the report to the student  Dolev in 'jbs'
