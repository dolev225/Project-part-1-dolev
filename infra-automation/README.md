=========================================================
welcome to the infar_automation project v1.0
=========================================================
over view :
this repository contains a python files that simulator a provisioning service configuration
* interactive with a user to create a VM entity
* validation with pydantic
* running a bash script to install ngnix & pydantic 
* saving the config in a configs/instances.json 
* logging at logs/logs.log
=========================================================
setup & execution instruction :
1) run the infra_simulator.py 
    it install nginx & pydantic up the latest version 
2) inreract with the code to add the machin
=========================================================
expected behavior
the script will create a machine list at 
if you wish to stop type stop the the machine 

machines are validated and then will save it in configs/instances.json

=========================================================
report:
if you see any bug or you wish to report en error
pleas send the the report to the student  Dolev in 'jbs'
